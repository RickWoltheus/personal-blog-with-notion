

/**
 *
 * This is an example router, you can delete this file and then update `../pages/api/trpc/[trpc].tsx`
 */
import { createRouter } from '../createRouter';

export const postRouter = createRouter().query()
